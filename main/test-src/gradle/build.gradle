
task listChangeFiles << {
  String changeListFile = System.getProperty("changeListFile")
  println "changeListFile: $changeListFile"

  File changedFiles = file(changeListFile)

  exec{
    commandLine = ["/bin/echo", "wibble"]
  }

  changedFiles.eachLine{
    println it
  }

}
